# LOCAL ONLY — copy to .env.render (gitignored). For migrations/seeds from your PC.
# Does NOT configure the hosted API. For Render Dashboard env vars see render.env.checklist.md
#
# Paste Render Connect → External Database URL:
DATABASE_URL=postgresql://transactpro_user:YOUR_PASSWORD@dpg-xxxxx-a.singapore-postgres.render.com:5432/transactpro_db_gidr?sslmode=require
