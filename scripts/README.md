# Scripts

One-off tooling (importers, data transforms, maintenance).

Examples to add later:

- Import `reference/Checklist Automation_.xlsx` → `backend/seeds/` JSON or SQL seed for `conditional_rules` / `document_types`.
- Regenerate `Docs/diagrams/full_erd.svg` from `Docs/diagrams/full_erd.mmd` (see `Docs/diagrams/README.md`).
